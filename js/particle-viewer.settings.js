/**
 * 3D Particicle Viewer
 * Parses the incomming JSON data into a settings object.
 * Each particle will be defined every given step.
 */
(function(){
	"use strict";
	
	var PV = window.PV = window.PV || {};
	
	/**
	 * 
	 */
	PV.parseSettings = function(){ };
	
})();